# TODO
- [x] Update src/controllers/authController.js: normalize login email (lowercase/trim) before DB query.
- [x] Add server-side logs to distinguish user-not-found vs password-mismatch (no sensitive data).
- [ ] Re-run tests/manual login and confirm 401 reason is resolved.


